using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpawnMult : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {/*
        screenLeft = Camera.main.ScreenToWorldPoint(new Vector3(0, 0, 0)).x;
        screenRight = Camera.main.ScreenToWorldPoint(new Vector3(Screen.width, 0, 0)).x;
        screenUp = Camera.main.ScreenToWorldPoint(new Vector3(Screen.height, 0, 0)).x;
        screenDown = Camera.main.ScreenToWorldPoint(new Vector3(Screen.width, 0, 0)).x;*/
    }

    // Update is called once per frame
    void Update()
    {

    }
}
